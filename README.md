### Url Shortener

A simple Go Project to shorten a big url by storing it in mysql db and then retrieving it.
The Ticket Server is hard-coded. But in real world, it will be a server that is generating a pseudo random integer which is then encoded and stored.
